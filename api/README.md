# api (placeholder)

Reserved for a local mock/sample Node.js transactions API, useful for
testing `ingestion/api_ingestion.py` end-to-end without the real service.
Expected contract is documented at the top of `ingestion/api_ingestion.py`.
